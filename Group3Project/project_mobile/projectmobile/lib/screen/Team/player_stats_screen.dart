import 'package:flutter/material.dart';

class PlayerStatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Nội dung thống kê cầu thủ',
        style: TextStyle(color: Colors.white, fontSize: 16),
      ),
    );
  }
}
