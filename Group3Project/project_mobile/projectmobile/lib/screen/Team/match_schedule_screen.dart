import 'package:flutter/material.dart';

class MatchScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Nội dung lịch thi đấu',
        style: TextStyle(color: Colors.white, fontSize: 16),
      ),
    );
  }
}
